import { View, Text } from 'react-native';

export default function AnaScreen() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Ana Tab (Dashboard)</Text>
    </View>
  );
}
